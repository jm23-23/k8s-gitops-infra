apiVersion: v1
kind: Service
metadata:
  name: moja-aplikacja-svc
  namespace: default
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000 # Zmiana z 80 na 3000
  selector:
    app: web